<UserControl x:Class="RouterPilot.Views.RouterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.WindowBackground}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="26" FontWeight="Bold" Text="Router"/>
        <TextBlock Grid.Row="1" Margin="0,4,0,18" Style="{DynamicResource Text.Muted}"
                   Text="Read-only router telemetry and diagnostics"/>
        <TabControl x:Name="RouterTabs" Grid.Row="2" Margin="0,0,0,14"
                    Style="{StaticResource RouterPilotTabControlStyle}"
                    ItemContainerStyle="{StaticResource RouterPilotTabItemStyle}">
            <TabItem Header="Overview">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <UniformGrid Columns="2" Margin="0,18,0,0">
                        <GroupBox Header="IDENTITY" Margin="0,0,12,12">
                            <StackPanel Margin="14">
                                <TextBlock Text="Model" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock FontSize="16" Text="{Binding RouterModel}"/>
                                <TextBlock Margin="0,10,0,0" Text="Hostname" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding Hostname}"/>
                                <TextBlock Margin="0,10,0,0" Text="Firmware" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding RouterFirmwareVersion}"/>
                                <TextBlock Margin="0,10,0,0" Text="Uptime" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding Uptime}"/>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="SYSTEM" Margin="12,0,0,12">
                            <StackPanel Margin="14">
                                <TextBlock Text="CPU" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock FontSize="16" Text="{Binding CpuUsageDisplay}"/>
                                <TextBlock Margin="0,10,0,0" Text="Memory" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding MemoryUsage}"/>
                                <TextBlock Margin="0,10,0,0" Text="Temperature" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding Temperature}"/>
                                <TextBlock Margin="0,10,0,0" Text="Storage" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding StorageUsage}"/>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="NETWORK" Margin="0,0,12,12">
                            <StackPanel Margin="14">
                                <TextBlock Text="Internet / WAN" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock FontSize="16" Text="{Binding InternetStatusText}"/>
                                <TextBlock Margin="0,10,0,0" Text="Public IP" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding PublicIpDisplay}"/>
                                <TextBlock Margin="0,10,0,0" Text="DNS server" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding DnsServer}"/>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="SERVICES" Margin="12,0,0,12">
                            <StackPanel Margin="14">
                                <TextBlock Text="AdGuard" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock FontSize="16" Text="{Binding AdGuardServiceDisplay}"/>
                                <TextBlock Margin="0,10,0,0" Text="VPN" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding VpnStatusText}"/>
                                <TextBlock Margin="0,10,0,0" Text="Wi-Fi summary" Style="{DynamicResource Text.Muted}"/>
                                <TextBlock Text="{Binding WifiSignalEmptyMessage}"/>
                            </StackPanel>
                        </GroupBox>
                    </UniformGrid>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Ports">
                <Grid Margin="0,18,0,0">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock x:Name="PortsStatus" Style="{DynamicResource Text.Muted}" Text="Loading read-only interface telemetry…"/><TextBlock x:Name="PortsSummary" Margin="0,3,0,0" Style="{DynamicResource Text.Secondary}"/><TextBlock x:Name="PortsAttention" Margin="0,3,0,0" Foreground="DarkOrange" TextWrapping="Wrap"/></StackPanel><Button Grid.Column="1" Content="Copy Ports Summary" Style="{DynamicResource Button.Secondary}" Click="CopyPortsSummary_Click"/></Grid>
                        <TextBlock x:Name="PortsSessionText" Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Session history (RouterPilot observations)"/>
                        <ListBox x:Name="PortsHistoryList" MaxHeight="110" BorderThickness="0" Background="Transparent"/>
                    </StackPanel>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="PortsList" Margin="0,10,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <GroupBox Header="{Binding FriendlyName}" Margin="0,0,0,10">
                                        <Grid Margin="14">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding LinkState}"/>
                                                <TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Muted}" Text="{Binding InterfaceName}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Style="{DynamicResource Text.Muted}" Text="Speed / Duplex"/>
                                                <TextBlock Text="{Binding SpeedDisplay}"/>
                                                <TextBlock Text="{Binding Duplex}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2">
                                                <TextBlock Style="{DynamicResource Text.Muted}" Text="RX / TX totals"/>
                                                <TextBlock Text="{Binding RxBytesDisplay}"/>
                                                <TextBlock Text="{Binding TxBytesDisplay}"/>
                                                <TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Muted}" Text="Errors / drops"/>
                                                <TextBlock Text="{Binding ErrorsDisplay}"/>
                                                <TextBlock Text="{Binding DropsDisplay}"/>
                                            </StackPanel>
                                        </Grid>
                                    </GroupBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="Wi-Fi">
                <Grid Margin="0,18,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock x:Name="WifiStatus" Style="{DynamicResource Text.Muted}" Text="Loading read-only Wi-Fi telemetry…"/><TextBlock x:Name="WifiSummary" Margin="0,3,0,0" Style="{DynamicResource Text.Secondary}"/><TextBlock x:Name="WifiAttention" Margin="0,3,0,0" Foreground="DarkOrange"/></StackPanel>
                            <Button Grid.Column="1" Content="Copy Wi-Fi summary" Style="{DynamicResource Button.Secondary}" Click="CopyWifiSummary_Click"/>
                        </Grid>
                        <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Session history (RouterPilot observations)"/>
                        <ListBox x:Name="WifiHistoryList" MaxHeight="100" BorderThickness="0" Background="Transparent"/>
                    </StackPanel>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="WifiRadiosList" Margin="0,10,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <GroupBox Header="{Binding DisplayName}" Margin="0,0,0,10">
                                        <StackPanel Margin="14">
                                            <TextBlock Style="{DynamicResource Text.Muted}" Text="Physical radio"/>
                                            <TextBlock Text="{Binding RadioDisplay}"/>
                                            <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding Networks}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Margin="0,0,0,10" Padding="0,0,0,10" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                                <StackPanel>
                                                                    <TextBlock Style="{DynamicResource Text.Muted}" Text="SSID / BSS"/>
                                                                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding Ssid}"/>
                                                                    <TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Muted}" Text="{Binding Interface}"/>
                                                                </StackPanel>
                                                                <StackPanel Grid.Column="1">
                                                                    <TextBlock Style="{DynamicResource Text.Muted}" Text="State"/>
                                                                    <TextBlock Text="{Binding StatusDisplay}"/>
                                                                    <TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Muted}" Text="Channel / width"/>
                                                                    <TextBlock Text="{Binding ChannelSummary, TargetNullValue=—}"/>
                                                                </StackPanel>
                                                                <StackPanel Grid.Column="2">
                                                                    <TextBlock Style="{DynamicResource Text.Muted}" Text="Mode"/>
                                                                    <TextBlock Text="{Binding HardwareMode, TargetNullValue=—}"/>
                                                                    <TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Muted}" Text="Clients / TX power"/>
                                                                    <TextBlock Text="{Binding ClientCountDisplay}"/>
                                                                    <TextBlock Text="{Binding TransmitPower, TargetNullValue=—}"/>
                                                                </StackPanel>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </GroupBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="Multi-WAN">
                <Grid Margin="0,18,0,0">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock x:Name="MultiWanSummary" FontSize="16" FontWeight="SemiBold" Text="Multi-WAN status unavailable"/><TextBlock x:Name="MultiWanStatus" Margin="0,4,0,0" Style="{DynamicResource Text.Muted}"/><TextBlock x:Name="MultiWanSessionText" Margin="0,3,0,0" Style="{DynamicResource Text.Secondary}"/></StackPanel>
                            <Button Grid.Column="1" Content="Copy Multi-WAN summary" Style="{DynamicResource Button.Secondary}" Click="CopyMultiWanSummary_Click"/>
                        </Grid>
                        <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Session history (RouterPilot observations)"/>
                        <ListBox x:Name="MultiWanHistoryList" MaxHeight="100" BorderThickness="0" Background="Transparent"/>
                    </StackPanel>
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="MultiWanList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <GroupBox Header="{Binding Name}" Margin="0,0,0,10">
                                        <Grid Margin="14">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <StackPanel><TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding RuntimeState}"/><TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Muted}" Text="{Binding InterfaceName}"/></StackPanel>
                                            <StackPanel Grid.Column="1"><TextBlock Style="{DynamicResource Text.Muted}" Text="Connection type"/><TextBlock Text="{Binding ConnectionType}"/><TextBlock Margin="0,4,0,0" Text="{Binding Gateway, TargetNullValue=—}"/></StackPanel>
                                            <StackPanel Grid.Column="2"><TextBlock Style="{DynamicResource Text.Muted}" Text="Role"/><TextBlock Text="{Binding IsActive, StringFormat=Active: {0}}"/><TextBlock Text="{Binding IsDefault, StringFormat=Default: {0}}"/></StackPanel>
                                        </Grid>
                                    </GroupBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="DNS">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,18,0,0">
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="DNS overview"/>
                        <TextBlock x:Name="DnsStatus" Margin="0,4,0,16" Style="{DynamicResource Text.Muted}" Text="DNS telemetry is currently unavailable."/>
                        <UniformGrid Columns="2">
                            <GroupBox Header="CONFIGURATION" Margin="0,0,12,12">
                                <StackPanel Margin="14">
                                    <TextBlock Style="{DynamicResource Text.Muted}" Text="Mode"/>
                                    <TextBlock x:Name="DnsModeText" FontSize="16" Text="—"/>
                                    <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Encrypted DNS"/>
                                    <TextBlock x:Name="DnsEncryptionText" Text="—"/>
                                    <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Upstream resolvers"/>
                                    <ItemsControl x:Name="DnsResolversList"/>
                                </StackPanel>
                            </GroupBox>
                            <GroupBox Header="SERVICES" Margin="12,0,0,12">
                                <StackPanel Margin="14">
                                    <TextBlock Style="{DynamicResource Text.Muted}" Text="DNS service"/>
                                    <TextBlock x:Name="DnsRuntimeText" FontSize="16" Text="—"/>
                                    <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="AdGuard Home"/>
                                    <TextBlock x:Name="DnsAdGuardText" Text="—"/>
                                    <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Handles client requests"/>
                                    <TextBlock x:Name="DnsHandlesText" Text="Unknown"/>
                                    <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="VPN DNS"/>
                                    <TextBlock x:Name="DnsVpnText" Text="—"/>
                                </StackPanel>
                            </GroupBox>
                        </UniformGrid>
                        <TextBlock Margin="0,4,0,12" Style="{DynamicResource Text.Muted}" Text="RouterPilot can display client DNS activity only when it is visible to AdGuard Home. Other DNS paths may not be observable." TextWrapping="Wrap"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Performance">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,18,0,0">
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="Performance"/>
                        <TextBlock x:Name="PerformanceStatus" Margin="0,4,0,16" Style="{DynamicResource Text.Muted}" Text="Loading router resource telemetry…"/>
                        <UniformGrid Columns="2">
                            <GroupBox Header="SYSTEM LOAD" Margin="0,0,12,12">
                                <StackPanel Margin="14">
                                    <TextBlock Style="{DynamicResource Text.Muted}" Text="CPU utilisation"/>
                                    <TextBlock x:Name="PerformanceCpuText" FontSize="16" Text="—"/>
                                    <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Load average (1 / 5 / 15 min)"/>
                                    <TextBlock x:Name="PerformanceLoadText" Text="—"/>
                                    <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Temperature"/>
                                    <TextBlock x:Name="PerformanceTemperatureText" Text="—"/>
                                </StackPanel>
                            </GroupBox>
                            <GroupBox Header="MEMORY / STORAGE" Margin="12,0,0,12">
                                <StackPanel Margin="14">
                                    <TextBlock Style="{DynamicResource Text.Muted}" Text="Memory"/>
                                    <TextBlock x:Name="PerformanceMemoryText" FontSize="16" Text="—"/>
                                    <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Storage"/>
                                    <TextBlock x:Name="PerformanceStorageText" Text="—" TextWrapping="Wrap"/>
                                    <TextBlock Margin="0,10,0,0" Style="{DynamicResource Text.Muted}" Text="Router uptime"/>
                                    <TextBlock x:Name="PerformanceUptimeText" Text="—"/>
                                </StackPanel>
                            </GroupBox>
                        </UniformGrid>
                        <TextBlock Margin="0,4,0,12" Style="{DynamicResource Text.Muted}" Text="Values use the same authoritative router telemetry shown elsewhere in RouterPilot. Load averages are not CPU percentages." TextWrapping="Wrap"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
