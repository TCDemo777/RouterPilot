<Window x:Class="RouterPilot.Views.FirmwareReleaseNotesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Release Notes" Width="680" Height="560" MinWidth="420" MinHeight="300"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource Brush.Surface}">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Release Notes" Style="{StaticResource SectionHeaderStyle}"/>
        <StackPanel Grid.Row="1" Margin="0,6,0,14">
            <TextBlock x:Name="ReleaseLabel" Style="{StaticResource Text.Secondary}"/>
            <TextBlock x:Name="ReleaseDate" Style="{StaticResource Text.Muted}" Margin="0,3,0,0"/>
        </StackPanel>
        <TextBox x:Name="NotesText" Grid.Row="2" IsReadOnly="True" IsReadOnlyCaretVisible="False" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Width="90" Height="36" Content="Close" Click="Close_Click" Style="{DynamicResource Button.Secondary}"/>
        </StackPanel>
    </Grid>
</Window>
