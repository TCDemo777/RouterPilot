<UserControl x:Class="RouterPilot.Views.NetworkHealthView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:RouterPilot.Converters" Background="{DynamicResource Brush.WindowBackground}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel Margin="0,0,0,16">
        <Border Margin="0,0,0,16" Style="{DynamicResource Card}"><StackPanel><TextBlock Style="{DynamicResource Text.SectionTitle}" Text="Network Health"/><TextBlock Margin="0,5,0,12" Style="{DynamicResource Text.Secondary}" Text="A read-only summary of state already refreshed by RouterPilot."/><TextBlock FontSize="22" FontWeight="SemiBold" Foreground="{Binding OverallColour}" Text="{Binding OverallStatus}"/><TextBlock Margin="0,5,0,0" Style="{DynamicResource Text.Muted}" Text="{Binding OverallDetail}"/></StackPanel></Border>
        <ItemsControl ItemsSource="{Binding Checks}"><ItemsControl.ItemTemplate><DataTemplate><Border Margin="0,0,0,10" Padding="16" Background="{DynamicResource Brush.SurfaceMuted}" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="10"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource Brush.TextPrimary}" Text="{Binding Title}"/><TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Muted}" TextWrapping="Wrap" Text="{Binding Detail}"/></StackPanel><StackPanel Grid.Column="1" Margin="16,0,0,0" HorizontalAlignment="Right"><TextBlock HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="{Binding Severity, Converter={x:Static local:StatusColourConverter.Instance}}" Text="{Binding Status}"/><Button Margin="0,9,0,0" Padding="10,4" Style="{DynamicResource Button.Secondary}" Content="View" Tag="{Binding NavigationTarget}" Click="Navigate_Click"/></StackPanel></Grid></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
    </StackPanel></ScrollViewer>
</UserControl>
